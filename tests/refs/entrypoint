#!/bin/sh
project_dir="CWD"
case "$(pwd -P)/" in
	"$project_dir/"*) ;;
	*) echo "safer: $0: outside project dir" 2>&1; exit 1 ;;
esac
podman run --rm -ti \
	--entrypoint '/bin/other_bin' \
	-v "CWD":"CWD" \
	-w "$(pwd -P)" \
	a_tool_image:latest "$@"
