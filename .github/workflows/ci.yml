---
name: Verify the codebase
on:
  push:
    branches: ["master"]
  pull_request:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: shellcheck
        run: make check
      - name: Tests
        run: make test
